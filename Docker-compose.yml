services:
  sistema-presenca:
    build: .
    container_name: monitor_assiduidade
    volumes:
      - .:/app
    tty: true
    stdin_open: true
    environment:
      - DISPLAY=host.docker.internal:0